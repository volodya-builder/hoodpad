{
  "name": "hood-chat-worker",
  "private": true,
  "version": "1.0.0",
  "description": "hood — чат холдеров с ИИ монеты (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node test/core.test.mjs"
  },
  "dependencies": {
    "viem": "^2.55.2"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
